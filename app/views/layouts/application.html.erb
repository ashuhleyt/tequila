<!DOCTYPE html>
<html>
  <head>
    <title>Tequila</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <section>
    <p><%= link_to "Drinks", "/drinks" %></p>
    <p><%= link_to "Bars", "/bars" %></p>
  </section>
  <body>
    <%= yield %>
  </body>
</html>
