<center>
  <h2>Welcome To <%= @bar.name %>s Page!


  <h4>
    Minimum Age: <%= @bar.minimum_age %>
    <br>
    Rating: <%= @bar.rating %>
    <br>
    ID: <%= @bar.id %>
    <br>
    Number of Drinks Offered: <%= @bar.drink_count %>
  </h4>

  <%= link_to "See #{@bar.name}s Drinks", "/bars/#{@bar.id}/drinks" %><br>
  <br>
  <%= link_to "Update #{@bar.name}", "/bars/#{@bar.id}/edit" %> 
  <br>
  <%= link_to "Delete #{@bar.name}", "/bars/#{@bar.id}/destroy", data: { confirm: 'Are you sure?' } %> 
</center>
